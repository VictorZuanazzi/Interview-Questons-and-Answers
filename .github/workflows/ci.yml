name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker build . -t ml-interview-qa:ci .
      - run: docker run ml-interview-qa:ci pytest .
