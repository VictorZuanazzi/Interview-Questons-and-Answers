name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@4
    - run docker build -t ml-internview-qa:ci
    - run docker run ml-internview-qa:ci pytest .
